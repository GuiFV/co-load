# Secrets template. Copy to .env (gitignored); docker compose reads it
# automatically from the repo root.

# Needed only for gated/private Hugging Face models served via vLLM.
HUGGING_FACE_HUB_TOKEN=
